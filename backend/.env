PROJECT_NAME="Price Tracker API"

# PostgreSQL Settings - NOTE: SERVER is now 'localhost'
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=price_tracker_db
POSTGRES_SERVER=localhost  # <-- CRITICAL CHANGE
POSTGRES_PORT=5432

# MongoDB Settings - NOTE: SERVER is now 'localhost'
MONGO_INITDB_ROOT_USERNAME=mongoadmin
MONGO_INITDB_ROOT_PASSWORD=mongopass
MONGO_SERVER=localhost     # <-- CRITICAL CHANGE
MONGO_PORT=27017
MONGO_DB_NAME=price_activity_log

# Redis Settings - We will install and run Redis locally
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Authentication Settings
SECRET_KEY=a_very_secret_key_that_should_be_long_and_random
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30